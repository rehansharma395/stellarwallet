{
  "name": "stellar-wallet-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build && rm -rf ../out && cp -r out ../out"
  }
}
